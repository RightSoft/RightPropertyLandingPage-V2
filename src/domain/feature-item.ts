export interface FeatureItem {
    title: string;
    description: string;
    image: string;
    icon: JSX.Element;
    mobileIcon : JSX.Element;
    desktopClass?: string;
    mobileClass?: string;
}